<style>
    th, td {
        padding: 10px;
    }
    th {
        text-align: right;
    }
</style>

<h1>Create or Update package:</h1>

{{ form_start(form) }}

<table>
    <tbody>
    <tr>
        <th>Title:</th>
        <td>{{ form_widget(form.title) }}</td>
    </tr>
    <tr>
        <th>description:</th>
        <td>{{ form_widget(form.description) }}</td>
    </tr>
    <tr>
        <th>package Price:</th>
        <td>{{ form_widget(form.price) }}</td>
    </tr>
    <tr>
        <th>package Currency:</th>
        <td>{{ form_widget(form.currency) }}</td>
    </tr>
    <tr>
        <th></th>
        <td>{{ form_widget(form.save) }}</td>
    </tr>
    </tbody>
</table>

{{ form_end(form) }}