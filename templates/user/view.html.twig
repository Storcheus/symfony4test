<style>
    th, td {
        padding: 10px;
    }
    th {
        text-align: right;
    }
</style>

<h1>user Created or Modified</h1>

<table>
    <tbody>
    <tr>
        <th>Name:</th>
        <td>{{ user.name }}</td>
    </tr>
    <tr>
        <th>Username:</th>
        <td>{{ user.username }}</td>
    </tr>
    <tr>
        <th>Email:</th>
        <td>{{ user.email }}</td>
    </tr>
    <tr>
        <th>Active:</th>
        <td>{{ user.active }}</td>
    </tr>
    </tbody>
</table>

<br/>
<a href="/user/show">View All</a>
<a href="/user/update/{{ user.id }}">Modify</a>
<a href="/">Homepage</a>